# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --no-restore -c Release

    - name: Pack Web API Host
      if: github.ref == 'refs/heads/main'
      run: tar -czf webapi-host.tar.gz -C Laraue.Apps.PdfQL.WebApi/bin/Release/net10.0 .

    - name: Upload Web API Host artifact
      if: github.ref == 'refs/heads/main'
      uses: actions/upload-artifact@v4
      with:
        name: webapi-host
        path: webapi-host.tar.gz
        retention-days: 7

  deploy:
    needs: build-and-test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: pdfql-production
    steps:
    - name: Notify deploy started
      run: |
        curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d text="🚀 Deploy started: PdfQL Web API Host (${{ github.sha }})"

    - name: Download artifact
      uses: actions/download-artifact@v4
      with:
        name: webapi-host

    - name: Upload host
      uses: marcodallasanta/ssh-scp-deploy@v1.2.0
      with:
       host: ${{ secrets.SSH_HOST }} # "example.com"
       port: ${{ secrets.SSH_PORT }} # 22
       user: ${{ secrets.SSH_USER }} # "user"
       password: ${{ secrets.SSH_PASSWORD }} # "password"
       local: webapi-host.tar.gz
       remote: "/home/laraue/backend"
       post_upload: |-
        cd /home/laraue/backend && find . -mindepth 1 ! -name webapi-host.tar.gz -delete &&
        tar -xzf webapi-host.tar.gz && rm webapi-host.tar.gz

    - name: Build container
      uses: appleboy/ssh-action@v1
      with:
       host: ${{ secrets.SSH_HOST }}
       port: ${{ secrets.SSH_PORT }}
       username: ${{ secrets.SSH_USER }}
       password: ${{ secrets.SSH_PASSWORD }}
       script: |
        set -e
        cd /home/laraue && docker compose build backendpdfql
        docker compose up -d --force-recreate backendpdfql

    - name: Notify deploy finished
      if: success()
      run: |
        curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d text="✅ Deploy finished: PdfQL Web API Host (${{ github.sha }})"

    - name: Notify deploy failed
      if: failure()
      run: |
        curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d text="❌ Deploy failed: PdfQL Web API Host (${{ github.sha }})"
